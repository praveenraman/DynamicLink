# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DeepLinkWithFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DeepLinkWithFirebase
   pod 'Firebase/Analytics'
   pod 'Firebase/DynamicLinks'
   pod 'Firebase/Core'


  target 'DeepLinkWithFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeepLinkWithFirebaseUITests' do
    # Pods for testing
  end

end
